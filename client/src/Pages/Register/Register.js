import React from 'react'
import RegisterComp from '../../Components/Register/RegisterComp'


const Register = ({history}) => {
    return (
        <div>
            <RegisterComp history={history}/>
        </div>
    )
}

export default Register
